package fjdb.interviews.algos;

import java.util.LinkedList;

public class SortLinkedList {



    public static void SortLinkedList(MyLinkedList<Integer> list) {

        /*
        iterate over list. Keep a treemap of node values to a count of them.
        get head of list. Assign this to temp node variable
        iterate over treemap. For each entry
            key is the node value. kvalue is the number of nodes to set to that value
            set node value to key. set node to node.next
            repeat kvalue times.
        end of tree map loop will have reassigned all node values

        time complexity O(n). Space complexity O(1) - worse case this is O(n) if all node values are different.
         */






    }
}
