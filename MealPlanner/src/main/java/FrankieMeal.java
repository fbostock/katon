public class FrankieMeal {

    /*
    xMax,
    yMax,
    minTiles,
    maxTiles = xMax,
    int tilesToAdd = random.next(minTiles, maxTiles)

    maxPosition = Math.min(xMax, yMax);//smallest of xMax, yMax, to ensure it'd be fine on any side

    //generate a random selection of positions from -MaxPosition to MaxPosition
    List<int> possiblePositions = new List<int>();
    for(int i=-maxPosition; i<=maxPosition; i++) {
        possiblePositions.add(i);
    }
    newList = ListUtil.randomise(possiblePositions);

    //
    for (i=0;i<tilesToAdd;i++){
    //add entries from newList to the list of positions to use. 
    }

    public Sides getSides() {}

public List<Position> makeCoords(int side, List<int> positions){...}
     */
}
